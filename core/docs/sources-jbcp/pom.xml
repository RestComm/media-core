
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<artifactId>mms-user-guide</artifactId>
		<groupId>org.mobicents.servers.media</groupId>
		<version>2.1.0.BETA2-SNAPSHOT</version>
	</parent>
	
	<artifactId>mms-user-guide-sources-jbcp</artifactId>
			
	<properties>
		<THIS.PLATFORM>JBoss Communications Platform</THIS.PLATFORM>
		<JEE.PLATFORM>JBoss Enterprise Application Platform</JEE.PLATFORM>
		<MANAGEMENT.PLATFORM>JBoss Operations Network</MANAGEMENT.PLATFORM>
		<THIS.PLATFORM.INSTALL_SECTION>Section-Install_JBCP.xml</THIS.PLATFORM.INSTALL_SECTION>
		
		<author.email.tom>twells (at) redhat.com</author.email.tom>
		<author.email.amit>abhayani (at) redhat.com</author.email.amit>
		<author.email.bartosz>bbaranow (at) redhat.com</author.email.bartosz>	
		<author.email.oleg>okulikov (at) redhat.com</author.email.oleg>
		<author.email.jared>jmorgan (at) redhat.com</author.email.jared>
		<author.email.silas>dhensley (at) redhat.com</author.email.silas>
		
	</properties>
	
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>${pom.groupId}</groupId>
				<artifactId>mms-user-guide-sources</artifactId>
				<version>${project.version}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>	
	<build>
		<resources>
			<resource>
        		<directory>${basedir}/src/main/resources</directory>
        		<filtering>true</filtering>
      		</resource>
      	</resources>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>unpack</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>${pom.groupId}</groupId>
									<artifactId>mms-user-guide-sources</artifactId>
									<version>${project.version}</version>
									<type>jar</type>
									<overWrite>true</overWrite>
									<outputDirectory>${basedir}/src/main/resources</outputDirectory>
								</artifactItem>
							</artifactItems>							
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
        <execution>
          <phase>install</phase>
          <goals>
            <goal>run</goal>
          </goals>
          <configuration>
            <tasks>
              <copy file="publican.cfg" todir="src/main/resources/" />
            </tasks>
          </configuration>
        </execution>        
					<execution>
						<id>clean-resources</id>
						<phase>clean</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<delete failonerror="false" includeemptydirs="true">
									<fileset dir="${basedir}/src" />
								</delete>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	
</project>
